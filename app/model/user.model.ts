export class User {
    id: string;
    name: string;
    phonenumber: number;
    address: string;

    constructor(options: any) {
        this.id = options.id;
        this.name = options.id;
        this.phonenumber = options.name;
        this.address = options.hasAC;
    }
}
